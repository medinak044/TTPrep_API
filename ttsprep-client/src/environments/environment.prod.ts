export const environment = {
    production: true,
    apiUrl: ("https://ttsprepapi.azurewebsites.net" + "/api")
};

//.../client/src> ng build --configuration=production
// Upload client build from "dist" folder
